<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="inventory_report_wizard_form_view" model="ir.ui.view">
            <field name="name">mulphico.inventory.report.wizard.form</field>
            <field name="model">mulphico.inventory.report.wizard</field>
            <field name="arch" type="xml">
                <form>
                    <group>
                        <group>
                            <field name="date_from"/>
                            <field name="warehouse_ids" widget="many2many_tags"/>
                            <field name="category_ids" widget="many2many_tags"/>
                        </group>
                        <group>
                            <field name="date_to"/>
                            <field name="location_ids" widget="many2many_tags"/>
                            <field name="product_ids" widget="many2many_tags"/>
                        </group>
                    </group>
                    <footer>
                        <button name="action_print_report" string="Print Report" type="object" class="btn-primary"/>
                        <button string="Cancel" class="btn-secondary" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>

    </data>
</odoo>